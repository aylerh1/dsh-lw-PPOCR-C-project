# 待实现
# 已实现
## g1.6
有没有类似本项目的占用空间少的下列模型：PP-DocLayout-L 版面分析(轻量)按区域路由,;表格走 SLANet+RT-DETR,公式走 FormulaNet-Plus-S(轻量);
识别后应转为md的zip：图片截取，同段合并等；
md预览不对，图片区域未截图，同段未合并；
带框图中文字丢失问题；布局识别图片截图范围不对是因为模型小性能不好么；
都先进行布局识别；再进行后续操作；导出json中要结合ocr和各个模型的结果；
文档转md：先用anydoc的pdf-inspector判别是否为图像需要ocr；
（1）需要ocr的都先进行布局识别；再进行后续操作；导出json中要结合ocr和各个模型的结果；
（2）不需要ocr的，pdf的也要进行布局识别和公式识别等，其他的直接转换；
不需要 OCR 的，仅pdf类型进行版面结构识别与表格/公式提取，其他的直接用anydoc转换；
截图判别布局识别对么，这figure画框的范围明显不对：err-files\image-files\法兰螺栓跨中分布.jpg的布局识别；
带框图还是无文字问题；转md的标题注意加上#。截图调试；
画框图在原内容上仅显示布局识别的类型即可；
md预览时figure内容不要显示图里的文字；
前端去掉内存占用描述如 (常驻 ~10MB) (~30MB resident)；readme.md中要突出占用和速度；
上传应不仅支持图片：还应支持其他所有anydoc支持的类型；

## g1.5
每页 OCR 耗时从 1.x 秒提速数十倍：采用原生 C/C++ 编译加速与 Go CGO 架构（编译 lw.PPOCR.C 原生库，AVX2多核加速，端到端耗时降至 20ms 级别，提速 50+ 倍，镜像从 155M 瘦身至 28.8M，常驻内存 ~30MB）；

## g1.4
画框位置不对问题；
提速：能后端实现的就后端实现；
容器常驻内存太大了，页面改为支持一张吧，能不能常驻内存到10M左右；

## g1.3
docker的restful实现占用内存太大了，100多M，是否用go的gin打包为二进制会小；

## g1.2
docker的弄个前端页面，可以识别图片，并返回内容，显示耗时；
docker的接口调用要求符合restful;

## g1.1
不用docker调试，本地调试；dist/要放入.ignore吧，要不太乱问题；/learn 通过 Composio 连接的 GitHub 账号创建公开仓库;
dsh: warning: dsh-lw-PPOCR-C-project declares no dsh.bundle — installed as a plain dependency, not a profile layer (a later update that gains one activates it automatically)
识别文字内容显示固定问题；


