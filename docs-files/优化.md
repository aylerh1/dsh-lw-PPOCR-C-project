# 待实现
# 已实现
## g1.2
docker的弄个前端页面，可以识别图片，并返回内容，显示耗时；
docker的接口调用要求符合restful;

## g1.1
不用docker调试，本地调试；dist/要放入.ignore吧，要不太乱问题；/learn 通过 Composio 连接的 GitHub 账号创建公开仓库;
dsh: warning: dsh-lw-PPOCR-C-project declares no dsh.bundle — installed as a plain dependency, not a profile layer (a later update that gains one activates it automatically)
识别文字内容显示固定问题；


